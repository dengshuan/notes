http {
# add this virtual host to http section
    server {
	listen 80;		#http default port
	server_name blog;	#blog should be mapped to localhost ip in /etc/hosts
	location / {
	    root /srv/http/notes; #publishing-directory
	    index sitemap.html;	#auto generated by org-publish-project
	}
    }
}
